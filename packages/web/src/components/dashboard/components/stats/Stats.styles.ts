import { styled } from "@stitches/react";

export const ChartTitle = styled("div", {
  fontWeight: "bold",
  marginBottom: 8,
});

export const Layout = styled("div", {
  display: "grid",
  rowGap: 16,
});
