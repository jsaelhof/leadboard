import { styled } from "@stitches/react";

export const Layout = styled("div", { display: "grid", rowGap: 16 });

export const Select = styled("select", { width: 200 });

export const Label = styled("label", { fontSize: 12 });
